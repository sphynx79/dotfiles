#!/bin/bash

cmd-present "$EDITOR" || EDITOR=nvim

case "$EDITOR" in
nvim | vim | nano | micro | hx | helix)
  exec launch-tui "$EDITOR" "$@"
  ;;
*)
  exec setsid app2unit.sh -t service "$EDITOR" "$@"
  ;;
esac
